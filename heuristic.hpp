#pragma once
#include <math.h>

namespace heuristic
{
    /*
     * A collection of heuristic functions (add more if needed later) 
     */ 
    unsigned int h( int x, int y, int goalX, int goalY );
}